# PostService 测试与验收

## 测试概览

| 指标 | 当前值 |
|------|--------|
| 测试类数 | 15 |
| 测试方法数 | 44（`mvn test`） |
| 是否依赖真实 PG/Redis | 否（MockBean / Mockito） |
| gRPC 端口 | 测试 profile 不启动 PostGrpcService |

## 关键测试类

| 类 | 覆盖 |
|----|------|
| PostGrpcServiceTest | gRPC 委托、鉴权 |
| PostGrpcServicePostTest | 帖子 4 RPC |
| PostGrpcServiceInteractionTest | 互动 4 RPC |
| PostGrpcServiceFeedTest | GetRecommendFeed |
| PostWriteServiceTest | 发帖 + 冷启动 + fanout |
| PostReadServiceTest | 详情缓存 + 实时计数 |
| PostLikeServiceTest | 点赞幂等 + delta |
| PostCommentServiceTest | 评论 CRUD |
| FeedServiceTest | 混排槽位 + Feed 组装 |
| FeedPoolRebuildServiceTest | 热门池重建 |
| PostFanoutServiceTest | 写扩散 fallback |
| PostStatFlushJobTest | Lua 刷盘 |
| PostServiceArchitectureTest | 关键文件 / Flyway / Redis 前缀 |
| PostServiceAcceptanceTest | Spring Context + 核心 Bean |
| PostServiceApplicationTests | 上下文加载 |

---

## 一键验收脚本

### Linux / macOS / Git Bash / WSL

```bash
# 仓库根目录
bash scripts/verify-post-service.sh
```

### Windows PowerShell

```powershell
.\scripts\verify-post-service.ps1
```

### 脚本 12 项检查

1. post-service 目录
2. proto 文件
3. 9 RPC 定义
4. Flyway V001/V002
5. 5 张表名
6. PostGrpcService
7. 5 核心 Service
8. 3 核心 Job
9. Redis yanshuqi 前缀
10. post-service 阶段未改 mobile-gateway
11. `mvn test`
12. `mvn package -DskipTests`

**不依赖** PostgreSQL / Redis 进程启动，**不启动** post-service 服务。

---

## 本地 Maven 命令

```bash
cd post-service
mvn test
mvn package -DskipTests
```

Windows 需 JDK 17+，示例：

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"
```

---

## 手工 Debug REST（需启动服务 + PG/Redis）

```bash
cd post-service && mvn spring-boot:run

# 健康检查
curl http://localhost:8084/health

# 发帖
curl -X POST http://localhost:8084/internal/debug/post \
  -H "Content-Type: application/json" -H "x-user-id: 10001" \
  -d '{"content":"hello","imageKeys":["post/10001/a.jpg"]}'

# 点赞
curl -X POST http://localhost:8084/internal/debug/post/{postId}/like -H "x-user-id: 10001"

# Feed
curl "http://localhost:8084/internal/debug/post/feed?pageSize=10&cursor=0:0" -H "x-user-id: 10001"

# 重建热门池
curl -X POST http://localhost:8084/internal/debug/post/feed/rebuild
```

---

## 最终验收命令清单

```bash
git status --short
bash scripts/verify-post-service.sh
cd post-service && mvn test && mvn package -DskipTests
```

Windows 追加：`.\scripts\verify-post-service.ps1`

---

## 回归范围

- proto 9 RPC 名称
- Flyway 表结构脚本
- 帖子 / 互动 / Feed 核心类存在
- Redis key 规范
- Maven 编译测试打包
- gateway 未被 post-service 阶段变更触及
