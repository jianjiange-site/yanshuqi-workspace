# mobile-gateway 接口地图

状态枚举：**已实现** | **契约占位** | **mock/test only** | **依赖下游未完成** | **不支持**

---

## Auth（6）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| POST `/api/v1/auth/send-sms-code` | 已实现 | gateway Redis + mock SMS | dev mock 123456 |
| POST `/api/v1/auth/login-device` | 已实现 | user-service Auth gRPC | 签发 JWT |
| POST `/api/v1/auth/login-phone` | 已实现 | user-service Auth gRPC | |
| POST `/api/v1/auth/login-third-party` | 已实现 | user-service Auth gRPC | |
| POST `/api/v1/auth/refresh` | 已实现 | gateway DB | 公开接口 |
| POST `/api/v1/auth/logout` | 已实现 | gateway Redis + DB | 需 JWT |

## Profile（2）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| POST `/api/v1/profile/onboarding` | 已实现 | user-service Profile | 需 JWT |
| PATCH `/api/v1/profile` | 已实现 | user-service Profile | 需 JWT |

## Upload（2）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| POST `/api/v1/upload/presign` | 已实现 | user-service Avatar | 需 JWT |
| POST `/api/v1/upload/confirm` | 已实现 | user-service Avatar | 需 JWT |

## Home（1）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| GET `/api/v1/home/card` | 已实现 | user-service HomeCard | 需 JWT |

## Match（7）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| GET `/api/v1/match/feed` | 已实现 | match-service | callerUserId in request |
| POST `/api/v1/match/swipe` | 已实现 | match-service | |
| POST `/api/v1/match/super-hi` | 已实现 | match-service | clientRequestId 透传 |
| GET `/api/v1/match/quota` | 已实现 | match-service | |
| GET `/api/v1/match/matches` | 已实现 | match-service | |
| POST `/api/v1/match/visit/{targetUserId}` | 已实现 | match-service | |
| GET `/api/v1/match/visits` | 已实现 | match-service | |

## Post（10）

| REST 接口 | 网关状态 | 下游依赖 | 说明 |
| --- | --- | --- | --- |
| POST `/api/v1/post` | 已实现 | post-service | metadata x-user-id |
| GET `/api/v1/post/feed` | 已实现 | post-service | |
| GET `/api/v1/post/user/{userId}` | 已实现 | post-service | |
| GET `/api/v1/post/{postId}` | 已实现 | post-service | |
| DELETE `/api/v1/post/{postId}` | 已实现 | post-service | |
| POST `/api/v1/post/{postId}/like` | 已实现 | post-service | |
| DELETE `/api/v1/post/{postId}/like` | 已实现 | post-service | |
| GET `/api/v1/post/{postId}/comment` | 已实现 | post-service | |
| POST `/api/v1/post/{postId}/comment` | 已实现 | post-service | |
| DELETE `/api/v1/post/comment/{commentId}` | 已实现 | post-service | |

## Payment（10）— 契约占位，非生产完成

| REST 接口 | 网关状态 | prod/dev | mock/test |
| --- | --- | --- | --- |
| GET `/api/v1/payment/products` | 契约占位 / 依赖下游未完成 | **10701 not ready** | mock 商品列表 |
| POST `/api/v1/payment/order` | 契约占位 | 10701 | mock order |
| POST `/api/v1/payment/verify` | 契约占位 | 10701 | mock VERIFIED |
| GET `/api/v1/payment/coins` | 契约占位 | 10701 | mock balance |
| GET `/api/v1/payment/coins/ledger` | 契约占位 | 10701 | mock 流水 |
| GET `/api/v1/payment/balance` | 契约占位 | 10701 | mock 余额 |
| GET `/api/v1/payment/subscription` | 契约占位 | 10701 | FREE/NONE |
| POST `/api/v1/payment/withdraw/bind` | 契约占位 | 10701 | mock accountId |
| POST `/api/v1/payment/withdraw` | 契约占位 | 10701 | mock withdrawId |
| GET `/api/v1/payment/history` | 契约占位 | 10701 | 空列表 |

实现类：`PaymentBffServiceImpl`（prod/dev） vs `MockPaymentBffServiceImpl`（mock/test only）。

## IM / Call / Callback — 契约占位

| REST 接口 | 网关状态 | prod/dev | mock/test |
| --- | --- | --- | --- |
| GET `/api/v1/im/token` | 契约占位 / 依赖下游未完成 | **10801** | mock-im-token-{userId} |
| POST `/api/v1/call/token` | 契约占位 | **10802** | mock LiveKit token |
| POST `/callback/openim/{callbackCommand}` | 回调占位 | CallbackResponse **10803** | actionCode=0 占位 |

**无 JWT**：callback 接口。

## Health（3）

| REST 接口 | 网关状态 | 说明 |
| --- | --- | --- |
| GET `/health` | 已实现 | 简单探活 |
| GET `/actuator/health` | 已实现 | Spring Actuator |
| GET `/health/infra` | 已实现 | 基建探测（InfraHealthController） |

## 统计

| 分类 | 数量 | 生产可用 |
| --- | ---: | --- |
| Auth/Profile/Upload/Home/Match/Post | 28 | **是** |
| Payment | 10 | **否**（契约占位） |
| IM/Call/Callback | 3 | **否**（契约占位） |
