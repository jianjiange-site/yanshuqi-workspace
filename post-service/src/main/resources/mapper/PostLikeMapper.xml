<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dating.post.mapper.PostLikeMapper">

    <!-- 点赞幂等 upsert：联合主键 (user_id, post_id)，仅 status 变化时更新 -->
    <insert id="upsertIfChanged">
        INSERT INTO post_likes (user_id, post_id, status, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{status}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
        ON CONFLICT (user_id, post_id) DO UPDATE
        SET status = EXCLUDED.status,
            updated_at = CURRENT_TIMESTAMP
        WHERE post_likes.status IS DISTINCT FROM EXCLUDED.status
    </insert>

    <select id="findStatus" resultType="java.lang.Integer">
        SELECT status
        FROM post_likes
        WHERE user_id = #{userId}
          AND post_id = #{postId}
    </select>

</mapper>
